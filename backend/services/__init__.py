"""External service integrations with pluggable backends.

Each service has a console/local backend (works out of the box) and a
production backend that activates when the relevant env var is set.
"""
