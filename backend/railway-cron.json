{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python scripts/release_payouts_cron.py",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
